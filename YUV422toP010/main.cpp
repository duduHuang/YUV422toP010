#include <iostream>
//#include "convert.h"

using namespace std;

int multiStream(int, char *[]);

int main(int argc, char* argv[]) {

    //convert(argc, argv);
	return multiStream(argc, argv);
}